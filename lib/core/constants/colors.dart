import 'package:flutter/material.dart';

class AppColors {
  // Brand
  static const Color primary = Color(0xFF2563EB);           // soft blue
  static const Color secondary = Color(0xFF66BB6A);
  static const Color primarySoft = Color(0xFFDBEAFE);       // very light blue
  static const Color primaryDark = Color(0xFF1D4ED8);

  static const Color onPrimary = Color(0xFFFFFFFF);
  static const Color onSecondary = Color(0xFFFFFFFF);
  // Background / surfaces
  static const Color background = Color(0xFFF3F4F6);        // gray-100
  static const Color surface = Color(0xFFFFFFFF);           // white
  static const Color surfaceSoft = Color(0xFFF9FAFB);       // almost white

  // Borders / lines
    static const Color border = Color(0xFFE5E7EB);           // gray-200

  // Text
  static const Color textPrimary = Color(0xFF111827);       // gray-900
  static const Color textSecondary = Color(0xFF6B7280);     // gray-500
  static const Color textMuted = Color(0xFF9CA3AF);         // gray-400

  // Status
  static const Color success = Color(0xFF16A34A);           // green-600
  static const Color successSoft = Color(0xFFD1FAE5);       // green-100
  static const Color error = Color(0xFFDC2626);             // red-600
  static const Color errorSoft = Color(0xFFFEE2E2);         // red-100;

  // Misc
  static const Color rating = Color(0xFFFBBF24);            // amber-400
}
